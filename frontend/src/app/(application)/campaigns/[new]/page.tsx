const CreateNewCampaignPage = () => {
	// ! TODO: Implement the Create New Campaign Page
	// * 1. Create a form to add a new campaign
	// * 2. Implement query param like if listIds=1,2,3 is passed, then show the selected lists in the form
	// * 3. Implement the form fields: name, description, tags, lists, schedule, message template, template message params

	return <section></section>
}

export default CreateNewCampaignPage
